import { css } from 'lit';

export const indexStyles = css`
    :host {
        margin: 0 auto;
        padding: 10px;
        overflow: hidden;
        width: 400px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }

    .button {
        width: 200px;
    }
`;
